#if 0
	shc Version 4.0.3, Generic Shell Script Compiler
	GNU GPL Version 3 Md Jahidul Hamid <jahidulhamid@yahoo.com>

	shc -rf mir_launchr.sh 
#endif

static  char data [] = 
#define      msg2_z	19
#define      msg2	((&data[4]))
	"\077\111\114\031\157\210\071\273\203\354\061\020\132\304\041\353"
	"\312\243\230\170\323\176\210\047\144\377\325"
#define      shll_z	10
#define      shll	((&data[29]))
	"\012\101\075\321\362\150\054\127\235\063\171\220"
#define      chk1_z	22
#define      chk1	((&data[40]))
	"\270\353\235\345\264\053\002\363\257\313\356\321\105\323\307\151"
	"\026\001\212\160\036\236\101\140\100"
#define      tst2_z	19
#define      tst2	((&data[64]))
	"\122\021\315\236\176\125\201\231\062\363\044\321\101\014\326\221"
	"\340\032\350\232\177\143\071"
#define      msg1_z	65
#define      msg1	((&data[97]))
	"\020\230\030\132\344\062\201\111\061\127\317\033\002\250\346\301"
	"\207\346\070\355\256\004\042\237\041\247\047\310\012\245\324\014"
	"\253\057\177\213\105\006\250\323\200\255\362\070\301\310\015\227"
	"\065\054\112\033\033\040\251\307\252\013\327\113\257\316\135\177"
	"\203\130\245\361\113\306\170\102\001\355\044\201\044\246\213\146"
	"\077\300\337\370\040\040\344\112\363"
#define      tst1_z	22
#define      tst1	((&data[180]))
	"\267\346\364\220\377\027\336\124\343\224\074\022\061\164\055\233"
	"\320\004\152\136\204\223\112\331\210\042\367\214"
#define      rlax_z	1
#define      rlax	((&data[204]))
	"\300"
#define      text_z	716
#define      text	((&data[353]))
	"\014\052\073\061\321\307\227\020\207\167\010\247\227\354\361\213"
	"\153\273\341\043\242\326\264\231\142\135\353\324\071\276\216\106"
	"\351\312\167\272\222\017\313\031\207\323\301\036\300\263\252\054"
	"\157\213\117\021\142\004\253\304\141\226\231\233\124\047\341\076"
	"\362\131\371\204\150\304\236\357\230\140\016\131\024\270\205\204"
	"\104\325\225\246\331\100\153\073\327\004\326\054\054\270\152\037"
	"\021\143\244\172\050\103\152\300\243\170\032\270\061\240\074\166"
	"\165\322\035\117\023\211\213\352\215\142\026\272\032\201\331\053"
	"\345\176\245\015\301\020\316\144\210\350\035\272\211\132\061\376"
	"\054\116\116\100\072\203\173\116\253\073\101\336\035\056\356\015"
	"\204\032\174\302\150\034\217\123\050\264\210\122\330\254\140\353"
	"\363\110\126\265\203\150\244\142\253\334\336\004\115\215\320\074"
	"\235\067\033\310\167\310\375\130\366\375\255\033\243\133\105\031"
	"\065\361\272\265\260\200\253\140\352\324\170\127\376\271\156\123"
	"\135\133\075\024\276\242\315\067\227\013\234\114\063\346\006\320"
	"\352\035\112\076\057\110\263\323\347\235\120\120\325\323\213\244"
	"\361\010\336\356\226\363\211\022\235\365\001\153\306\116\313\344"
	"\342\140\247\234\107\353\266\337\006\207\273\360\163\366\046\322"
	"\206\176\132\020\221\140\271\351\070\334\104\236\220\166\174\321"
	"\133\046\173\374\266\341\071\207\240\145\061\063\332\340\111\230"
	"\340\012\062\241\210\013\372\277\133\135\062\305\042\075\315\314"
	"\161\137\255\074\253\041\350\076\004\026\125\352\050\004\057\264"
	"\234\021\310\322\123\035\303\157\077\001\075\174\144\304\125\276"
	"\264\370\354\121\253\327\234\315\110\054\346\312\130\027\276\114"
	"\063\151\126\137\154\025\164\175\006\324\377\060\025\076\023\366"
	"\210\121\222\375\200\074\102\233\151\212\137\121\264\210\353\267"
	"\056\124\054\211\271\350\247\275\062\116\021\072\054\304\164\036"
	"\272\147\174\205\326\017\305\177\045\102\057\264\364\126\300\163"
	"\026\140\236\153\324\340\250\152\202\212\050\300\144\314\232\237"
	"\102\021\360\033\026\125\154\242\322\105\177\325\136\116\075\050"
	"\061\133\111\141\041\030\311\162\263\270\375\177\134\010\207\151"
	"\356\273\364\107\227\236\001\012\215\340\174\245\355\336\035\100"
	"\322\250\020\340\232\002\352\352\102\176\164\011\070\223\200\232"
	"\233\261\343\133\207\037\330\170\011\200\203\223\145\145\267\176"
	"\276\106\142\161\124\127\346\311\316\303\246\115\276\054\275\151"
	"\165\361\323\320\034\161\324\217\052\354\263\122\256\177\077\361"
	"\177\120\264\347\204\176\212\303\201\340\027\247\302\320\104\061"
	"\260\054\112\127\366\053\156\042\365\334\276\330\301\037\152\340"
	"\173\014\011\067\106\256\123\266\207\175\133\125\076\212\376\102"
	"\223\140\322\371\335\207\313\102\007\313\115\061\151\135\307\330"
	"\237\012\255\164\202\350\073\172\350\107\014\331\056\301\076\152"
	"\217\142\376\050\242\361\257\073\107\262\130\140\316\202\170\226"
	"\112\124\006\117\106\137\226\100\141\255\265\330\177\127\212\360"
	"\074\330\371\160\332\240\153\363\377\226\231\062\257\354\275\015"
	"\153\366\271\216\217\012\246\075\071\375\216\260\305\234\370\121"
	"\316\057\125\153\304\305\277\335\271\023\220\264\372\364\263\074"
	"\065\336\244\014\121\036\123\064\123\004\171\174\312\265\315\216"
	"\231\077\352\162\337\201\211\324\007\105\347\237\006\345\250\120"
	"\277\075\041\060\362\267\030\016\311\362\136\026\202\062\004\033"
	"\022\303\247\233\332\120\150\366\305\022\257\124\233\341\176\352"
	"\020\237\255\016\334\115\175\224\347\071\167\074\206\146\012\302"
	"\155\320\312\004\162\273\041\000\212\141\332\075\364\025\044\356"
	"\216\060\000\077\203\156\054\171\304\117\056\005\002\142\233\222"
	"\146\077\236\201\356\040\335\210\277\147\100\243\365\111\066\045"
	"\327\331\053\145\073\101\040\126\303\371\202\250\167\050\266\071"
	"\070\204\236\300\155\273\173\366\025\254\365\102\373\103\203\322"
	"\035\256\070\131\360\130\257\263\122\061\133\312\131\022\003\221"
	"\226\241\122\004\134\316\372\162\172\360\265\165\063\071\110\121"
	"\347\200\252\327\331\132\213\053\213\347\365\345\371\370\167\220"
	"\232\311\224\366\230\216\151\023\177\037\210\262\130\321\004\077"
	"\122\256\027\053\010\243\127\224\212\114\171\204\105\360\024\337"
	"\272\250\326\123\067\100\146\266\137\356\151\270\300\155\370\023"
	"\034\020\076"
#define      date_z	1
#define      date	((&data[1200]))
	"\315"
#define      opts_z	1
#define      opts	((&data[1201]))
	"\307"
#define      inlo_z	3
#define      inlo	((&data[1202]))
	"\335\133\164"
#define      xecc_z	15
#define      xecc	((&data[1208]))
	"\336\200\337\177\270\017\147\167\332\353\276\324\312\113\101\232"
	"\316\166"
#define      lsto_z	1
#define      lsto	((&data[1223]))
	"\121"
#define      chk2_z	19
#define      chk2	((&data[1226]))
	"\206\331\126\235\222\023\025\020\236\362\047\250\360\257\161\300"
	"\056\236\055\311\012\070\107"
#define      pswd_z	256
#define      pswd	((&data[1259]))
	"\143\100\231\210\364\057\101\062\022\164\365\073\172\354\361\164"
	"\061\127\335\377\041\113\267\166\310\232\271\172\043\267\274\141"
	"\047\011\356\237\052\326\203\245\132\026\062\324\003\044\111\064"
	"\173\046\064\235\161\353\023\072\206\315\265\251\205\161\013\254"
	"\173\371\114\245\320\317\113\052\346\175\376\351\241\110\036\035"
	"\156\122\272\340\076\316\033\304\233\320\156\040\102\171\315\275"
	"\163\031\143\103\351\256\156\317\053\155\270\314\265\326\351\043"
	"\050\244\004\147\162\037\053\016\357\231\056\061\023\373\357\206"
	"\025\122\312\376\000\070\315\054\245\205\371\132\134\343\176\205"
	"\207\202\354\372\242\030\010\221\262\066\303\305\062\263\114\107"
	"\005\026\105\006\117\023\062\364\231\054\117\365\017\316\172\227"
	"\121\147\221\363\177\232\205\061\321\111\366\003\374\102\113\002"
	"\131\220\010\250\243\073\235\074\147\355\062\167\274\255\016\015"
	"\024\240\001\223\072\206\305\013\320\273\016\314\376\131\316\130"
	"\352\327\000\216\022\236\313\172\213\375\361\107\252\000\125\277"
	"\240\126\122\332\335\027\345\255\323\364\172\322\115\111\052\070"
	"\040\053\306\063\311\222\255\125\220\237\235\072\230\315\104\166"
	"\115\044\250\005\104\100\165\305\306\116\375\016\224\055\150\370"
	"\156\001"/* End of data[] */;
#define      hide_z	4096
#define SETUID 0	/* Define as 1 to call setuid(0) at start of script */
#define DEBUGEXEC	0	/* Define as 1 to debug execvp calls */
#define TRACEABLE	1	/* Define as 1 to enable ptrace the executable */
#define HARDENING	0	/* Define as 1 to disable ptrace/dump the executable */
#define BUSYBOXON	0	/* Define as 1 to enable work with busybox */

#if HARDENING
static const char * shc_x[] = {
"/*",
" * Copyright 2019 - Intika <intika@librefox.org>",
" * Replace ******** with secret read from fd 21",
" * Also change arguments location of sub commands (sh script commands)",
" * gcc -Wall -fpic -shared -o shc_secret.so shc_secret.c -ldl",
" */",
"",
"#define _GNU_SOURCE /* needed to get RTLD_NEXT defined in dlfcn.h */",
"#define PLACEHOLDER \"********\"",
"#include <dlfcn.h>",
"#include <stdlib.h>",
"#include <string.h>",
"#include <unistd.h>",
"#include <stdio.h>",
"#include <signal.h>",
"",
"static char secret[128000]; //max size",
"typedef int (*pfi)(int, char **, char **);",
"static pfi real_main;",
"",
"// copy argv to new location",
"char **copyargs(int argc, char** argv){",
"    char **newargv = malloc((argc+1)*sizeof(*argv));",
"    char *from,*to;",
"    int i,len;",
"",
"    for(i = 0; i<argc; i++){",
"        from = argv[i];",
"        len = strlen(from)+1;",
"        to = malloc(len);",
"        memcpy(to,from,len);",
"        // zap old argv space",
"        memset(from,'\\0',len);",
"        newargv[i] = to;",
"        argv[i] = 0;",
"    }",
"    newargv[argc] = 0;",
"    return newargv;",
"}",
"",
"static int mymain(int argc, char** argv, char** env) {",
"    //fprintf(stderr, \"Inject main argc = %d\\n\", argc);",
"    return real_main(argc, copyargs(argc,argv), env);",
"}",
"",
"int __libc_start_main(int (*main) (int, char**, char**),",
"                      int argc,",
"                      char **argv,",
"                      void (*init) (void),",
"                      void (*fini)(void),",
"                      void (*rtld_fini)(void),",
"                      void (*stack_end)){",
"    static int (*real___libc_start_main)() = NULL;",
"    int n;",
"",
"    if (!real___libc_start_main) {",
"        real___libc_start_main = dlsym(RTLD_NEXT, \"__libc_start_main\");",
"        if (!real___libc_start_main) abort();",
"    }",
"",
"    n = read(21, secret, sizeof(secret));",
"    if (n > 0) {",
"      int i;",
"",
"    if (secret[n - 1] == '\\n') secret[--n] = '\\0';",
"    for (i = 1; i < argc; i++)",
"        if (strcmp(argv[i], PLACEHOLDER) == 0)",
"          argv[i] = secret;",
"    }",
"",
"    real_main = main;",
"",
"    return real___libc_start_main(mymain, argc, argv, init, fini, rtld_fini, stack_end);",
"}",
"",
0};
#endif /* HARDENING */

/* rtc.c */

#include <sys/stat.h>
#include <sys/types.h>

#include <errno.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <time.h>
#include <unistd.h>

/* 'Alleged RC4' */

static unsigned char stte[256], indx, jndx, kndx;

/*
 * Reset arc4 stte. 
 */
void stte_0(void)
{
	indx = jndx = kndx = 0;
	do {
		stte[indx] = indx;
	} while (++indx);
}

/*
 * Set key. Can be used more than once. 
 */
void key(void * str, int len)
{
	unsigned char tmp, * ptr = (unsigned char *)str;
	while (len > 0) {
		do {
			tmp = stte[indx];
			kndx += tmp;
			kndx += ptr[(int)indx % len];
			stte[indx] = stte[kndx];
			stte[kndx] = tmp;
		} while (++indx);
		ptr += 256;
		len -= 256;
	}
}

/*
 * Crypt data. 
 */
void arc4(void * str, int len)
{
	unsigned char tmp, * ptr = (unsigned char *)str;
	while (len > 0) {
		indx++;
		tmp = stte[indx];
		jndx += tmp;
		stte[indx] = stte[jndx];
		stte[jndx] = tmp;
		tmp += stte[indx];
		*ptr ^= stte[tmp];
		ptr++;
		len--;
	}
}

/* End of ARC4 */

#if HARDENING

#include <sys/ptrace.h>
#include <sys/wait.h>
#include <signal.h>
#include <sys/prctl.h>
#define PR_SET_PTRACER 0x59616d61

/* Seccomp Sandboxing Init */
#include <stdlib.h>
#include <stdio.h>
#include <stddef.h>
#include <string.h>
#include <unistd.h>
#include <errno.h>

#include <sys/types.h>
#include <sys/prctl.h>
#include <sys/syscall.h>
#include <sys/socket.h>

#include <linux/filter.h>
#include <linux/seccomp.h>
#include <linux/audit.h>

#define ArchField offsetof(struct seccomp_data, arch)

#define Allow(syscall) \
    BPF_JUMP(BPF_JMP+BPF_JEQ+BPF_K, SYS_##syscall, 0, 1), \
    BPF_STMT(BPF_RET+BPF_K, SECCOMP_RET_ALLOW)

struct sock_filter filter[] = {
    /* validate arch */
    BPF_STMT(BPF_LD+BPF_W+BPF_ABS, ArchField),
    BPF_JUMP( BPF_JMP+BPF_JEQ+BPF_K, AUDIT_ARCH_X86_64, 1, 0),
    BPF_STMT(BPF_RET+BPF_K, SECCOMP_RET_KILL),

    /* load syscall */
    BPF_STMT(BPF_LD+BPF_W+BPF_ABS, offsetof(struct seccomp_data, nr)),

    /* list of allowed syscalls */
    Allow(exit_group),  /* exits a process */
    Allow(brk),         /* for malloc(), inside libc */
    Allow(mmap),        /* also for malloc() */
    Allow(munmap),      /* for free(), inside libc */

    /* and if we don't match above, die */
    BPF_STMT(BPF_RET+BPF_K, SECCOMP_RET_KILL),
};
struct sock_fprog filterprog = {
    .len = sizeof(filter)/sizeof(filter[0]),
    .filter = filter
};

/* Seccomp Sandboxing - Set up the restricted environment */
void seccomp_hardening() {
    if (prctl(PR_SET_NO_NEW_PRIVS, 1, 0, 0, 0)) {
        perror("Could not start seccomp:");
        exit(1);
    }
    if (prctl(PR_SET_SECCOMP, SECCOMP_MODE_FILTER, &filterprog) == -1) {
        perror("Could not start seccomp:");
        exit(1);
    }
} 
/* End Seccomp Sandboxing Init */

void shc_x_file() {
    FILE *fp;
    int line = 0;

    if ((fp = fopen("/tmp/shc_x.c", "w")) == NULL ) {exit(1); exit(1);}
    for (line = 0; shc_x[line]; line++)	fprintf(fp, "%s\n", shc_x[line]);
    fflush(fp);fclose(fp);
}

int make() {
	char * cc, * cflags, * ldflags;
    char cmd[4096];

	cc = getenv("CC");
	if (!cc) cc = "cc";

	sprintf(cmd, "%s %s -o %s %s", cc, "-Wall -fpic -shared", "/tmp/shc_x.so", "/tmp/shc_x.c -ldl");
	if (system(cmd)) {remove("/tmp/shc_x.c"); return -1;}
	remove("/tmp/shc_x.c"); return 0;
}

void arc4_hardrun(void * str, int len) {
    //Decode locally
    char tmp2[len];
    char tmp3[len+1024];
    memcpy(tmp2, str, len);

	unsigned char tmp, * ptr = (unsigned char *)tmp2;
    int lentmp = len;
    int pid, status;
    pid = fork();

    shc_x_file();
    if (make()) {exit(1);}

    setenv("LD_PRELOAD","/tmp/shc_x.so",1);

    if(pid==0) {

        //Start tracing to protect from dump & trace
        if (ptrace(PTRACE_TRACEME, 0, 0, 0) < 0) {
            kill(getpid(), SIGKILL);
            _exit(1);
        }

        //Decode Bash
        while (len > 0) {
            indx++;
            tmp = stte[indx];
            jndx += tmp;
            stte[indx] = stte[jndx];
            stte[jndx] = tmp;
            tmp += stte[indx];
            *ptr ^= stte[tmp];
            ptr++;
            len--;
        }

        //Do the magic
        sprintf(tmp3, "%s %s", "'********' 21<<<", tmp2);

        //Exec bash script //fork execl with 'sh -c'
        system(tmp2);

        //Empty script variable
        memcpy(tmp2, str, lentmp);

        //Clean temp
        remove("/tmp/shc_x.so");

        //Sinal to detach ptrace
        ptrace(PTRACE_DETACH, 0, 0, 0);
        exit(0);
    }
    else {wait(&status);}

    /* Seccomp Sandboxing - Start */
    seccomp_hardening();

    exit(0);
}
#endif /* HARDENING */

/*
 * Key with file invariants. 
 */
int key_with_file(char * file)
{
	struct stat statf[1];
	struct stat control[1];

	if (stat(file, statf) < 0)
		return -1;

	/* Turn on stable fields */
	memset(control, 0, sizeof(control));
	control->st_ino = statf->st_ino;
	control->st_dev = statf->st_dev;
	control->st_rdev = statf->st_rdev;
	control->st_uid = statf->st_uid;
	control->st_gid = statf->st_gid;
	control->st_size = statf->st_size;
	control->st_mtime = statf->st_mtime;
	control->st_ctime = statf->st_ctime;
	key(control, sizeof(control));
	return 0;
}

#if DEBUGEXEC
void debugexec(char * sh11, int argc, char ** argv)
{
	int i;
	fprintf(stderr, "shll=%s\n", sh11 ? sh11 : "<null>");
	fprintf(stderr, "argc=%d\n", argc);
	if (!argv) {
		fprintf(stderr, "argv=<null>\n");
	} else { 
		for (i = 0; i <= argc ; i++)
			fprintf(stderr, "argv[%d]=%.60s\n", i, argv[i] ? argv[i] : "<null>");
	}
}
#endif /* DEBUGEXEC */

void rmarg(char ** argv, char * arg)
{
	for (; argv && *argv && *argv != arg; argv++);
	for (; argv && *argv; argv++)
		*argv = argv[1];
}

void chkenv_end(void);

int chkenv(int argc)
{
	char buff[512];
	unsigned long mask, m;
	int l, a, c;
	char * string;
	extern char ** environ;

	mask = (unsigned long)getpid();
	stte_0();
	 key(&chkenv, (void*)&chkenv_end - (void*)&chkenv);
	 key(&data, sizeof(data));
	 key(&mask, sizeof(mask));
	arc4(&mask, sizeof(mask));
	sprintf(buff, "x%lx", mask);
	string = getenv(buff);
#if DEBUGEXEC
	fprintf(stderr, "getenv(%s)=%s\n", buff, string ? string : "<null>");
#endif
	l = strlen(buff);
	if (!string) {
		/* 1st */
		sprintf(&buff[l], "=%lu %d", mask, argc);
		putenv(strdup(buff));
		return 0;
	}
	c = sscanf(string, "%lu %d%c", &m, &a, buff);
	if (c == 2 && m == mask) {
		/* 3rd */
		rmarg(environ, &string[-l - 1]);
		return 1 + (argc - a);
	}
	return -1;
}

void chkenv_end(void){}

#if HARDENING

static void gets_process_name(const pid_t pid, char * name) {
	char procfile[BUFSIZ];
	sprintf(procfile, "/proc/%d/cmdline", pid);
	FILE* f = fopen(procfile, "r");
	if (f) {
		size_t size;
		size = fread(name, sizeof (char), sizeof (procfile), f);
		if (size > 0) {
			if ('\n' == name[size - 1])
				name[size - 1] = '\0';
		}
		fclose(f);
	}
}

void hardening() {
    prctl(PR_SET_DUMPABLE, 0);
    prctl(PR_SET_PTRACER, -1);

    int pid = getppid();
    char name[256] = {0};
    gets_process_name(pid, name);

    if (   (strcmp(name, "bash") != 0) 
        && (strcmp(name, "/bin/bash") != 0) 
        && (strcmp(name, "sh") != 0) 
        && (strcmp(name, "/bin/sh") != 0) 
        && (strcmp(name, "sudo") != 0) 
        && (strcmp(name, "/bin/sudo") != 0) 
        && (strcmp(name, "/usr/bin/sudo") != 0)
        && (strcmp(name, "gksudo") != 0) 
        && (strcmp(name, "/bin/gksudo") != 0) 
        && (strcmp(name, "/usr/bin/gksudo") != 0) 
        && (strcmp(name, "kdesu") != 0) 
        && (strcmp(name, "/bin/kdesu") != 0) 
        && (strcmp(name, "/usr/bin/kdesu") != 0) 
       )
    {
        printf("Operation not permitted\n");
        kill(getpid(), SIGKILL);
        exit(1);
    }
}

#endif /* HARDENING */

#if !TRACEABLE

#define _LINUX_SOURCE_COMPAT
#include <sys/ptrace.h>
#include <sys/types.h>
#include <sys/wait.h>
#include <fcntl.h>
#include <signal.h>
#include <stdio.h>
#include <unistd.h>

#if !defined(PT_ATTACHEXC) /* New replacement for PT_ATTACH */
   #if !defined(PTRACE_ATTACH) && defined(PT_ATTACH)
       #define PT_ATTACHEXC	PT_ATTACH
   #elif defined(PTRACE_ATTACH)
       #define PT_ATTACHEXC PTRACE_ATTACH
   #endif
#endif

void untraceable(char * argv0)
{
	char proc[80];
	int pid, mine;

	switch(pid = fork()) {
	case  0:
		pid = getppid();
		/* For problematic SunOS ptrace */
#if defined(__FreeBSD__)
		sprintf(proc, "/proc/%d/mem", (int)pid);
#else
		sprintf(proc, "/proc/%d/as",  (int)pid);
#endif
		close(0);
		mine = !open(proc, O_RDWR|O_EXCL);
		if (!mine && errno != EBUSY)
			mine = !ptrace(PT_ATTACHEXC, pid, 0, 0);
		if (mine) {
			kill(pid, SIGCONT);
		} else {
			perror(argv0);
			kill(pid, SIGKILL);
		}
		_exit(mine);
	case -1:
		break;
	default:
		if (pid == waitpid(pid, 0, 0))
			return;
	}
	perror(argv0);
	_exit(1);
}
#endif /* !TRACEABLE */

char * xsh(int argc, char ** argv)
{
	char * scrpt;
	int ret, i, j;
	char ** varg;
	char * me = argv[0];
	if (me == NULL) { me = getenv("_"); }
	if (me == 0) { fprintf(stderr, "E: neither argv[0] nor $_ works."); exit(1); }

	ret = chkenv(argc);
	stte_0();
	 key(pswd, pswd_z);
	arc4(msg1, msg1_z);
	arc4(date, date_z);
	if (date[0] && (atoll(date)<time(NULL)))
		return msg1;
	arc4(shll, shll_z);
	arc4(inlo, inlo_z);
	arc4(xecc, xecc_z);
	arc4(lsto, lsto_z);
	arc4(tst1, tst1_z);
	 key(tst1, tst1_z);
	arc4(chk1, chk1_z);
	if ((chk1_z != tst1_z) || memcmp(tst1, chk1, tst1_z))
		return tst1;
	arc4(msg2, msg2_z);
	if (ret < 0)
		return msg2;
	varg = (char **)calloc(argc + 10, sizeof(char *));
	if (!varg)
		return 0;
	if (ret) {
		arc4(rlax, rlax_z);
		if (!rlax[0] && key_with_file(shll))
			return shll;
		arc4(opts, opts_z);
#if HARDENING
	    arc4_hardrun(text, text_z);
	    exit(0);
       /* Seccomp Sandboxing - Start */
       seccomp_hardening();
#endif
		arc4(text, text_z);
		arc4(tst2, tst2_z);
		 key(tst2, tst2_z);
		arc4(chk2, chk2_z);
		if ((chk2_z != tst2_z) || memcmp(tst2, chk2, tst2_z))
			return tst2;
		/* Prepend hide_z spaces to script text to hide it. */
		scrpt = malloc(hide_z + text_z);
		if (!scrpt)
			return 0;
		memset(scrpt, (int) ' ', hide_z);
		memcpy(&scrpt[hide_z], text, text_z);
	} else {			/* Reexecute */
		if (*xecc) {
			scrpt = malloc(512);
			if (!scrpt)
				return 0;
			sprintf(scrpt, xecc, me);
		} else {
			scrpt = me;
		}
	}
	j = 0;
#if BUSYBOXON
	varg[j++] = "busybox";
	varg[j++] = "sh";
#else
	varg[j++] = argv[0];		/* My own name at execution */
#endif
	if (ret && *opts)
		varg[j++] = opts;	/* Options on 1st line of code */
	if (*inlo)
		varg[j++] = inlo;	/* Option introducing inline code */
	varg[j++] = scrpt;		/* The script itself */
	if (*lsto)
		varg[j++] = lsto;	/* Option meaning last option */
	i = (ret > 1) ? ret : 0;	/* Args numbering correction */
	while (i < argc)
		varg[j++] = argv[i++];	/* Main run-time arguments */
	varg[j] = 0;			/* NULL terminated array */
#if DEBUGEXEC
	debugexec(shll, j, varg);
#endif
	execvp(shll, varg);
	return shll;
}

int main(int argc, char ** argv)
{
#if SETUID
   setuid(0);
#endif
#if DEBUGEXEC
	debugexec("main", argc, argv);
#endif
#if HARDENING
	hardening();
#endif
#if !TRACEABLE
	untraceable(argv[0]);
#endif
	argv[1] = xsh(argc, argv);
	fprintf(stderr, "%s%s%s: %s\n", argv[0],
		errno ? ": " : "",
		errno ? strerror(errno) : "",
		argv[1] ? argv[1] : "<null>"
	);
	return 1;
}
