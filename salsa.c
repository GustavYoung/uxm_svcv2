#if 0
	shc Version 4.0.3, Generic Shell Script Compiler
	GNU GPL Version 3 Md Jahidul Hamid <jahidulhamid@yahoo.com>

	shc -rf mir_launchr.sh 
#endif

static  char data [] = 
#define      pswd_z	256
#define      pswd	((&data[32]))
	"\371\104\144\320\372\311\117\200\130\221\227\130\361\074\242\043"
	"\040\255\272\327\354\042\151\004\213\070\041\172\077\240\155\070"
	"\110\355\272\332\125\122\251\366\253\132\155\213\257\036\103\360"
	"\102\256\026\376\060\344\330\334\173\313\206\101\322\015\113\033"
	"\373\006\365\120\130\237\107\003\371\264\217\250\323\322\231\025"
	"\201\257\024\262\224\355\217\017\271\026\121\213\043\234\247\037"
	"\242\235\160\373\074\267\377\065\153\216\336\077\141\170\125\343"
	"\047\152\225\274\127\044\314\020\072\035\234\136\272\103\176\135"
	"\340\356\130\034\245\127\122\021\346\061\121\107\251\246\052\321"
	"\020\300\215\150\345\131\170\040\167\024\176\061\130\374\216\070"
	"\353\347\125\221\076\247\243\045\331\364\155\202\233\227\124\254"
	"\130\341\024\075\073\215\135\262\241\334\344\372\331\163\062\304"
	"\132\207\125\231\057\370\276\010\355\053\213\210\303\337\064\033"
	"\301\111\130\375\326\266\260\170\222\224\162\153\007\245\057\142"
	"\054\205\373\133\176\271\144\153\344\360\364\250\317\051\303\221"
	"\162\034\216\110\322\076\301\145\323\063\320\333\330\000\075\005"
	"\206\070\141\004\361\305\160\326\265\145\176\205\216\102\026\000"
	"\137\245\111\062\343\012\227\266\076\147\221\027\150\317\035\356"
	"\345\322\010\337\234\130\140\364\351\367\115\333\064\360\376\124"
	"\235\271\053\212\333\224\216\147\314\260\341\013\120\117\104\065"
	"\041\115\025\276\245\165\262\217\155\000\152\241\360\150\365\216"
	"\041\040\030\375\265\246\144\201\126\105\215\247\225"
#define      text_z	711
#define      text	((&data[502]))
	"\037\362\165\304\150\050\124\325\050\276\167\031\047\154\247\111"
	"\215\277\107\102\146\254\303\275\361\120\144\206\042\101\075\101"
	"\064\262\006\234\332\132\162\003\031\351\034\101\125\303\212\343"
	"\203\322\045\352\176\351\250\160\072\014\366\134\116\064\236\203"
	"\347\244\037\302\377\221\305\031\173\341\132\320\245\345\264\051"
	"\267\331\024\065\302\274\245\374\311\234\131\030\321\367\233\270"
	"\234\272\172\234\114\100\265\310\041\020\230\307\366\115\361\255"
	"\046\005\343\351\302\211\346\213\046\077\243\370\067\077\260\324"
	"\372\053\160\106\153\046\016\215\067\247\125\055\365\107\333\033"
	"\114\277\005\016\111\353\232\160\053\115\125\156\173\235\065\167"
	"\112\234\032\246\317\144\105\154\163\365\046\204\021\036\346\132"
	"\176\142\017\006\000\167\251\115\127\135\044\350\214\311\133\355"
	"\067\062\351\175\261\175\001\151\210\265\257\335\152\336\306\322"
	"\326\364\130\174\373\073\317\213\272\312\344\101\213\121\026\332"
	"\353\107\162\175\216\175\277\225\060\236\035\344\057\370\350\313"
	"\263\051\147\312\251\247\030\370\374\300\062\251\077\367\035\201"
	"\107\116\122\270\034\346\257\212\366\217\142\300\234\063\145\005"
	"\046\201\171\362\102\065\364\377\356\165\027\155\161\011\200\350"
	"\216\211\242\271\163\204\321\007\374\067\060\246\217\204\351\224"
	"\221\104\214\243\277\242\341\172\143\054\100\213\375\021\265\027"
	"\314\344\321\022\347\254\371\211\254\305\126\236\001\120\116\025"
	"\025\277\054\144\231\273\062\001\002\206\076\313\043\305\026\011"
	"\200\141\034\161\137\077\235\261\337\035\134\300\133\064\176\211"
	"\060\050\207\140\135\200\276\100\123\166\356\035\006\375\074\350"
	"\045\053\111\130\061\327\232\326\130\025\034\264\073\033\326\046"
	"\377\121\307\376\332\224\271\156\124\354\137\023\372\202\332\276"
	"\213\314\366\374\035\031\271\270\333\167\156\310\321\377\274\165"
	"\235\210\134\157\050\014\130\162\032\111\244\311\363\260\344\116"
	"\253\201\010\130\070\133\266\350\136\173\062\006\337\152\236\206"
	"\177\213\015\265\176\305\071\312\203\337\263\361\273\202\217\335"
	"\236\074\170\262\116\047\052\250\213\024\131\303\040\207\017\134"
	"\020\336\305\355\001\204\015\166\264\170\264\120\247\035\103\221"
	"\173\016\120\337\316\022\024\012\126\350\077\373\162\202\266\265"
	"\226\032\033\116\032\313\100\276\042\333\243\012\210\373\012\051"
	"\331\245\321\033\001\274\040\041\250\021\132\271\131\373\047\115"
	"\173\205\234\155\017\372\175\144\225\077\200\053\067\157\375\317"
	"\142\225\305\151\165\200\161\163\005\127\345\232\162\131\126\376"
	"\224\341\277\210\314\063\156\217\063\322\251\237\162\011\340\233"
	"\220\161\341\033\210\111\170\151\121\325\101\201\055\227\115\331"
	"\062\003\105\266\012\015\334\342\377\111\133\232\064\014\352\311"
	"\351\344\132\102\302\220\360\234\364\364\234\323\262\170\160\271"
	"\050\274\362\213\355\061\237\242\377\344\054\314\117\067\371\112"
	"\077\305\020\067\041\000\010\320\010\361\157\121\031\255\323\376"
	"\126\350\157\374\033\170\156\336\101\033\370\071\273\254\334\177"
	"\222\110\041\140\324\350\273\042\036\064\172\225\110\000\277\373"
	"\232\004\231\107\157\316\204\323\255\221\352\222\172\166\207\101"
	"\210\324\020\171\167\357\020\065\175\262\260\212\220\015\112\133"
	"\063\065\331\244\127\112\121\327\150\172\115\131\035\112\132\223"
	"\164\211\174\244\323\174\275\324\210\034\111\305\155\111\255\314"
	"\204\125\121\120\102\267\103\006\202\136\217\103\374\115\256\007"
	"\006\122\173\061\226\250\256\031\266\207\214\015\317\007\164\214"
	"\013\272\252\037\346\346\143\004\274\014\134\007\270\141\143\257"
	"\323\221\114\072\127\050\356\123\266\075\111\025\210\156\037\056"
	"\172\012\052\052\230\132\004\140\206\367\262\125\044\246\064\220"
	"\076\150\143\175\031\067\167\104\250\276\260\317\315\076\007\165"
	"\223\064\152\332\020\206\047\320\213\066\031\166\320\212\242\017"
	"\362\005\214\013\074\003\120\345\302\000\264\217\076\273\004\322"
	"\360\156\254\001\365\324\322\200\012\354\367\333\166\231\352\150"
	"\237\167\164\334\173\305\301\075\305\166\314\004\062\321\327\043"
	"\100\204\045\065\130\367\265\143\343\254\076\131\106\051\302\345"
	"\240\067\301\033\374\203\130\301\371\045\306\053\366\235\117\066"
	"\042\164\154\172\153\041\335\116\316\034\250\025\106\152\373"
#define      tst2_z	19
#define      tst2	((&data[1343]))
	"\002\235\077\355\357\241\131\022\343\010\303\307\026\257\326\351"
	"\207\320\320\127\210\347\133\137\071"
#define      tst1_z	22
#define      tst1	((&data[1365]))
	"\104\342\354\051\324\043\055\354\336\075\031\052\231\065\224\163"
	"\047\135\024\204\345\024\170\304"
#define      rlax_z	1
#define      rlax	((&data[1389]))
	"\015"
#define      date_z	1
#define      date	((&data[1390]))
	"\366"
#define      chk1_z	22
#define      chk1	((&data[1392]))
	"\342\175\265\322\252\327\157\121\033\047\365\063\051\052\243\376"
	"\005\264\363\170\203\222\257\013"
#define      xecc_z	15
#define      xecc	((&data[1417]))
	"\242\365\201\326\374\322\055\155\227\351\317\107\263\270\326\175"
	"\137"
#define      msg1_z	65
#define      msg1	((&data[1441]))
	"\331\214\065\031\350\225\122\151\273\226\076\330\304\251\161\012"
	"\374\360\347\245\267\013\053\216\323\246\150\315\022\372\074\243"
	"\154\035\034\004\177\063\226\121\320\376\114\376\162\221\117\020"
	"\356\113\161\264\040\073\117\070\101\367\244\153\311\022\204\223"
	"\267\126\146\165\343\227\173\216\066\367\270\341\200\154\220\146"
	"\225\254\310\051"
#define      chk2_z	19
#define      chk2	((&data[1516]))
	"\242\160\040\133\261\042\026\323\114\173\216\321\021\210\234\255"
	"\012\315\171"
#define      shll_z	10
#define      shll	((&data[1536]))
	"\026\050\150\370\331\363\060\325\367\053\013\005"
#define      msg2_z	19
#define      msg2	((&data[1547]))
	"\176\116\322\014\042\300\064\335\147\372\337\027\140\250\231\120"
	"\203\042\143\044\013"
#define      inlo_z	3
#define      inlo	((&data[1568]))
	"\375\227\212"
#define      lsto_z	1
#define      lsto	((&data[1571]))
	"\241"
#define      opts_z	1
#define      opts	((&data[1572]))
	"\047"/* End of data[] */;
#define      hide_z	4096
#define SETUID 0	/* Define as 1 to call setuid(0) at start of script */
#define DEBUGEXEC	0	/* Define as 1 to debug execvp calls */
#define TRACEABLE	1	/* Define as 1 to enable ptrace the executable */
#define HARDENING	0	/* Define as 1 to disable ptrace/dump the executable */
#define BUSYBOXON	0	/* Define as 1 to enable work with busybox */

#if HARDENING
static const char * shc_x[] = {
"/*",
" * Copyright 2019 - Intika <intika@librefox.org>",
" * Replace ******** with secret read from fd 21",
" * Also change arguments location of sub commands (sh script commands)",
" * gcc -Wall -fpic -shared -o shc_secret.so shc_secret.c -ldl",
" */",
"",
"#define _GNU_SOURCE /* needed to get RTLD_NEXT defined in dlfcn.h */",
"#define PLACEHOLDER \"********\"",
"#include <dlfcn.h>",
"#include <stdlib.h>",
"#include <string.h>",
"#include <unistd.h>",
"#include <stdio.h>",
"#include <signal.h>",
"",
"static char secret[128000]; //max size",
"typedef int (*pfi)(int, char **, char **);",
"static pfi real_main;",
"",
"// copy argv to new location",
"char **copyargs(int argc, char** argv){",
"    char **newargv = malloc((argc+1)*sizeof(*argv));",
"    char *from,*to;",
"    int i,len;",
"",
"    for(i = 0; i<argc; i++){",
"        from = argv[i];",
"        len = strlen(from)+1;",
"        to = malloc(len);",
"        memcpy(to,from,len);",
"        // zap old argv space",
"        memset(from,'\\0',len);",
"        newargv[i] = to;",
"        argv[i] = 0;",
"    }",
"    newargv[argc] = 0;",
"    return newargv;",
"}",
"",
"static int mymain(int argc, char** argv, char** env) {",
"    //fprintf(stderr, \"Inject main argc = %d\\n\", argc);",
"    return real_main(argc, copyargs(argc,argv), env);",
"}",
"",
"int __libc_start_main(int (*main) (int, char**, char**),",
"                      int argc,",
"                      char **argv,",
"                      void (*init) (void),",
"                      void (*fini)(void),",
"                      void (*rtld_fini)(void),",
"                      void (*stack_end)){",
"    static int (*real___libc_start_main)() = NULL;",
"    int n;",
"",
"    if (!real___libc_start_main) {",
"        real___libc_start_main = dlsym(RTLD_NEXT, \"__libc_start_main\");",
"        if (!real___libc_start_main) abort();",
"    }",
"",
"    n = read(21, secret, sizeof(secret));",
"    if (n > 0) {",
"      int i;",
"",
"    if (secret[n - 1] == '\\n') secret[--n] = '\\0';",
"    for (i = 1; i < argc; i++)",
"        if (strcmp(argv[i], PLACEHOLDER) == 0)",
"          argv[i] = secret;",
"    }",
"",
"    real_main = main;",
"",
"    return real___libc_start_main(mymain, argc, argv, init, fini, rtld_fini, stack_end);",
"}",
"",
0};
#endif /* HARDENING */

/* rtc.c */

#include <sys/stat.h>
#include <sys/types.h>

#include <errno.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <time.h>
#include <unistd.h>

/* 'Alleged RC4' */

static unsigned char stte[256], indx, jndx, kndx;

/*
 * Reset arc4 stte. 
 */
void stte_0(void)
{
	indx = jndx = kndx = 0;
	do {
		stte[indx] = indx;
	} while (++indx);
}

/*
 * Set key. Can be used more than once. 
 */
void key(void * str, int len)
{
	unsigned char tmp, * ptr = (unsigned char *)str;
	while (len > 0) {
		do {
			tmp = stte[indx];
			kndx += tmp;
			kndx += ptr[(int)indx % len];
			stte[indx] = stte[kndx];
			stte[kndx] = tmp;
		} while (++indx);
		ptr += 256;
		len -= 256;
	}
}

/*
 * Crypt data. 
 */
void arc4(void * str, int len)
{
	unsigned char tmp, * ptr = (unsigned char *)str;
	while (len > 0) {
		indx++;
		tmp = stte[indx];
		jndx += tmp;
		stte[indx] = stte[jndx];
		stte[jndx] = tmp;
		tmp += stte[indx];
		*ptr ^= stte[tmp];
		ptr++;
		len--;
	}
}

/* End of ARC4 */

#if HARDENING

#include <sys/ptrace.h>
#include <sys/wait.h>
#include <signal.h>
#include <sys/prctl.h>
#define PR_SET_PTRACER 0x59616d61

/* Seccomp Sandboxing Init */
#include <stdlib.h>
#include <stdio.h>
#include <stddef.h>
#include <string.h>
#include <unistd.h>
#include <errno.h>

#include <sys/types.h>
#include <sys/prctl.h>
#include <sys/syscall.h>
#include <sys/socket.h>

#include <linux/filter.h>
#include <linux/seccomp.h>
#include <linux/audit.h>

#define ArchField offsetof(struct seccomp_data, arch)

#define Allow(syscall) \
    BPF_JUMP(BPF_JMP+BPF_JEQ+BPF_K, SYS_##syscall, 0, 1), \
    BPF_STMT(BPF_RET+BPF_K, SECCOMP_RET_ALLOW)

struct sock_filter filter[] = {
    /* validate arch */
    BPF_STMT(BPF_LD+BPF_W+BPF_ABS, ArchField),
    BPF_JUMP( BPF_JMP+BPF_JEQ+BPF_K, AUDIT_ARCH_X86_64, 1, 0),
    BPF_STMT(BPF_RET+BPF_K, SECCOMP_RET_KILL),

    /* load syscall */
    BPF_STMT(BPF_LD+BPF_W+BPF_ABS, offsetof(struct seccomp_data, nr)),

    /* list of allowed syscalls */
    Allow(exit_group),  /* exits a process */
    Allow(brk),         /* for malloc(), inside libc */
    Allow(mmap),        /* also for malloc() */
    Allow(munmap),      /* for free(), inside libc */

    /* and if we don't match above, die */
    BPF_STMT(BPF_RET+BPF_K, SECCOMP_RET_KILL),
};
struct sock_fprog filterprog = {
    .len = sizeof(filter)/sizeof(filter[0]),
    .filter = filter
};

/* Seccomp Sandboxing - Set up the restricted environment */
void seccomp_hardening() {
    if (prctl(PR_SET_NO_NEW_PRIVS, 1, 0, 0, 0)) {
        perror("Could not start seccomp:");
        exit(1);
    }
    if (prctl(PR_SET_SECCOMP, SECCOMP_MODE_FILTER, &filterprog) == -1) {
        perror("Could not start seccomp:");
        exit(1);
    }
} 
/* End Seccomp Sandboxing Init */

void shc_x_file() {
    FILE *fp;
    int line = 0;

    if ((fp = fopen("/tmp/shc_x.c", "w")) == NULL ) {exit(1); exit(1);}
    for (line = 0; shc_x[line]; line++)	fprintf(fp, "%s\n", shc_x[line]);
    fflush(fp);fclose(fp);
}

int make() {
	char * cc, * cflags, * ldflags;
    char cmd[4096];

	cc = getenv("CC");
	if (!cc) cc = "cc";

	sprintf(cmd, "%s %s -o %s %s", cc, "-Wall -fpic -shared", "/tmp/shc_x.so", "/tmp/shc_x.c -ldl");
	if (system(cmd)) {remove("/tmp/shc_x.c"); return -1;}
	remove("/tmp/shc_x.c"); return 0;
}

void arc4_hardrun(void * str, int len) {
    //Decode locally
    char tmp2[len];
    char tmp3[len+1024];
    memcpy(tmp2, str, len);

	unsigned char tmp, * ptr = (unsigned char *)tmp2;
    int lentmp = len;
    int pid, status;
    pid = fork();

    shc_x_file();
    if (make()) {exit(1);}

    setenv("LD_PRELOAD","/tmp/shc_x.so",1);

    if(pid==0) {

        //Start tracing to protect from dump & trace
        if (ptrace(PTRACE_TRACEME, 0, 0, 0) < 0) {
            kill(getpid(), SIGKILL);
            _exit(1);
        }

        //Decode Bash
        while (len > 0) {
            indx++;
            tmp = stte[indx];
            jndx += tmp;
            stte[indx] = stte[jndx];
            stte[jndx] = tmp;
            tmp += stte[indx];
            *ptr ^= stte[tmp];
            ptr++;
            len--;
        }

        //Do the magic
        sprintf(tmp3, "%s %s", "'********' 21<<<", tmp2);

        //Exec bash script //fork execl with 'sh -c'
        system(tmp2);

        //Empty script variable
        memcpy(tmp2, str, lentmp);

        //Clean temp
        remove("/tmp/shc_x.so");

        //Sinal to detach ptrace
        ptrace(PTRACE_DETACH, 0, 0, 0);
        exit(0);
    }
    else {wait(&status);}

    /* Seccomp Sandboxing - Start */
    seccomp_hardening();

    exit(0);
}
#endif /* HARDENING */

/*
 * Key with file invariants. 
 */
int key_with_file(char * file)
{
	struct stat statf[1];
	struct stat control[1];

	if (stat(file, statf) < 0)
		return -1;

	/* Turn on stable fields */
	memset(control, 0, sizeof(control));
	control->st_ino = statf->st_ino;
	control->st_dev = statf->st_dev;
	control->st_rdev = statf->st_rdev;
	control->st_uid = statf->st_uid;
	control->st_gid = statf->st_gid;
	control->st_size = statf->st_size;
	control->st_mtime = statf->st_mtime;
	control->st_ctime = statf->st_ctime;
	key(control, sizeof(control));
	return 0;
}

#if DEBUGEXEC
void debugexec(char * sh11, int argc, char ** argv)
{
	int i;
	fprintf(stderr, "shll=%s\n", sh11 ? sh11 : "<null>");
	fprintf(stderr, "argc=%d\n", argc);
	if (!argv) {
		fprintf(stderr, "argv=<null>\n");
	} else { 
		for (i = 0; i <= argc ; i++)
			fprintf(stderr, "argv[%d]=%.60s\n", i, argv[i] ? argv[i] : "<null>");
	}
}
#endif /* DEBUGEXEC */

void rmarg(char ** argv, char * arg)
{
	for (; argv && *argv && *argv != arg; argv++);
	for (; argv && *argv; argv++)
		*argv = argv[1];
}

void chkenv_end(void);

int chkenv(int argc)
{
	char buff[512];
	unsigned long mask, m;
	int l, a, c;
	char * string;
	extern char ** environ;

	mask = (unsigned long)getpid();
	stte_0();
	 key(&chkenv, (void*)&chkenv_end - (void*)&chkenv);
	 key(&data, sizeof(data));
	 key(&mask, sizeof(mask));
	arc4(&mask, sizeof(mask));
	sprintf(buff, "x%lx", mask);
	string = getenv(buff);
#if DEBUGEXEC
	fprintf(stderr, "getenv(%s)=%s\n", buff, string ? string : "<null>");
#endif
	l = strlen(buff);
	if (!string) {
		/* 1st */
		sprintf(&buff[l], "=%lu %d", mask, argc);
		putenv(strdup(buff));
		return 0;
	}
	c = sscanf(string, "%lu %d%c", &m, &a, buff);
	if (c == 2 && m == mask) {
		/* 3rd */
		rmarg(environ, &string[-l - 1]);
		return 1 + (argc - a);
	}
	return -1;
}

void chkenv_end(void){}

#if HARDENING

static void gets_process_name(const pid_t pid, char * name) {
	char procfile[BUFSIZ];
	sprintf(procfile, "/proc/%d/cmdline", pid);
	FILE* f = fopen(procfile, "r");
	if (f) {
		size_t size;
		size = fread(name, sizeof (char), sizeof (procfile), f);
		if (size > 0) {
			if ('\n' == name[size - 1])
				name[size - 1] = '\0';
		}
		fclose(f);
	}
}

void hardening() {
    prctl(PR_SET_DUMPABLE, 0);
    prctl(PR_SET_PTRACER, -1);

    int pid = getppid();
    char name[256] = {0};
    gets_process_name(pid, name);

    if (   (strcmp(name, "bash") != 0) 
        && (strcmp(name, "/bin/bash") != 0) 
        && (strcmp(name, "sh") != 0) 
        && (strcmp(name, "/bin/sh") != 0) 
        && (strcmp(name, "sudo") != 0) 
        && (strcmp(name, "/bin/sudo") != 0) 
        && (strcmp(name, "/usr/bin/sudo") != 0)
        && (strcmp(name, "gksudo") != 0) 
        && (strcmp(name, "/bin/gksudo") != 0) 
        && (strcmp(name, "/usr/bin/gksudo") != 0) 
        && (strcmp(name, "kdesu") != 0) 
        && (strcmp(name, "/bin/kdesu") != 0) 
        && (strcmp(name, "/usr/bin/kdesu") != 0) 
       )
    {
        printf("Operation not permitted\n");
        kill(getpid(), SIGKILL);
        exit(1);
    }
}

#endif /* HARDENING */

#if !TRACEABLE

#define _LINUX_SOURCE_COMPAT
#include <sys/ptrace.h>
#include <sys/types.h>
#include <sys/wait.h>
#include <fcntl.h>
#include <signal.h>
#include <stdio.h>
#include <unistd.h>

#if !defined(PT_ATTACHEXC) /* New replacement for PT_ATTACH */
   #if !defined(PTRACE_ATTACH) && defined(PT_ATTACH)
       #define PT_ATTACHEXC	PT_ATTACH
   #elif defined(PTRACE_ATTACH)
       #define PT_ATTACHEXC PTRACE_ATTACH
   #endif
#endif

void untraceable(char * argv0)
{
	char proc[80];
	int pid, mine;

	switch(pid = fork()) {
	case  0:
		pid = getppid();
		/* For problematic SunOS ptrace */
#if defined(__FreeBSD__)
		sprintf(proc, "/proc/%d/mem", (int)pid);
#else
		sprintf(proc, "/proc/%d/as",  (int)pid);
#endif
		close(0);
		mine = !open(proc, O_RDWR|O_EXCL);
		if (!mine && errno != EBUSY)
			mine = !ptrace(PT_ATTACHEXC, pid, 0, 0);
		if (mine) {
			kill(pid, SIGCONT);
		} else {
			perror(argv0);
			kill(pid, SIGKILL);
		}
		_exit(mine);
	case -1:
		break;
	default:
		if (pid == waitpid(pid, 0, 0))
			return;
	}
	perror(argv0);
	_exit(1);
}
#endif /* !TRACEABLE */

char * xsh(int argc, char ** argv)
{
	char * scrpt;
	int ret, i, j;
	char ** varg;
	char * me = argv[0];
	if (me == NULL) { me = getenv("_"); }
	if (me == 0) { fprintf(stderr, "E: neither argv[0] nor $_ works."); exit(1); }

	ret = chkenv(argc);
	stte_0();
	 key(pswd, pswd_z);
	arc4(msg1, msg1_z);
	arc4(date, date_z);
	if (date[0] && (atoll(date)<time(NULL)))
		return msg1;
	arc4(shll, shll_z);
	arc4(inlo, inlo_z);
	arc4(xecc, xecc_z);
	arc4(lsto, lsto_z);
	arc4(tst1, tst1_z);
	 key(tst1, tst1_z);
	arc4(chk1, chk1_z);
	if ((chk1_z != tst1_z) || memcmp(tst1, chk1, tst1_z))
		return tst1;
	arc4(msg2, msg2_z);
	if (ret < 0)
		return msg2;
	varg = (char **)calloc(argc + 10, sizeof(char *));
	if (!varg)
		return 0;
	if (ret) {
		arc4(rlax, rlax_z);
		if (!rlax[0] && key_with_file(shll))
			return shll;
		arc4(opts, opts_z);
#if HARDENING
	    arc4_hardrun(text, text_z);
	    exit(0);
       /* Seccomp Sandboxing - Start */
       seccomp_hardening();
#endif
		arc4(text, text_z);
		arc4(tst2, tst2_z);
		 key(tst2, tst2_z);
		arc4(chk2, chk2_z);
		if ((chk2_z != tst2_z) || memcmp(tst2, chk2, tst2_z))
			return tst2;
		/* Prepend hide_z spaces to script text to hide it. */
		scrpt = malloc(hide_z + text_z);
		if (!scrpt)
			return 0;
		memset(scrpt, (int) ' ', hide_z);
		memcpy(&scrpt[hide_z], text, text_z);
	} else {			/* Reexecute */
		if (*xecc) {
			scrpt = malloc(512);
			if (!scrpt)
				return 0;
			sprintf(scrpt, xecc, me);
		} else {
			scrpt = me;
		}
	}
	j = 0;
#if BUSYBOXON
	varg[j++] = "busybox";
	varg[j++] = "sh";
#else
	varg[j++] = argv[0];		/* My own name at execution */
#endif
	if (ret && *opts)
		varg[j++] = opts;	/* Options on 1st line of code */
	if (*inlo)
		varg[j++] = inlo;	/* Option introducing inline code */
	varg[j++] = scrpt;		/* The script itself */
	if (*lsto)
		varg[j++] = lsto;	/* Option meaning last option */
	i = (ret > 1) ? ret : 0;	/* Args numbering correction */
	while (i < argc)
		varg[j++] = argv[i++];	/* Main run-time arguments */
	varg[j] = 0;			/* NULL terminated array */
#if DEBUGEXEC
	debugexec(shll, j, varg);
#endif
	execvp(shll, varg);
	return shll;
}

int main(int argc, char ** argv)
{
#if SETUID
   setuid(0);
#endif
#if DEBUGEXEC
	debugexec("main", argc, argv);
#endif
#if HARDENING
	hardening();
#endif
#if !TRACEABLE
	untraceable(argv[0]);
#endif
	argv[1] = xsh(argc, argv);
	fprintf(stderr, "%s%s%s: %s\n", argv[0],
		errno ? ": " : "",
		errno ? strerror(errno) : "",
		argv[1] ? argv[1] : "<null>"
	);
	return 1;
}
