#if 0
	shc Version 4.0.3, Generic Shell Script Compiler
	GNU GPL Version 3 Md Jahidul Hamid <jahidulhamid@yahoo.com>

	shc -vrf mir_launchr.sh 
#endif

static  char data [] = 
#define      msg2_z	19
#define      msg2	((&data[1]))
	"\031\161\141\003\045\211\350\047\126\042\237\200\226\364\304\034"
	"\071\050\305\071\176\120\112"
#define      text_z	705
#define      text	((&data[89]))
	"\111\233\222\275\172\075\342\055\215\363\012\256\067\206\260\243"
	"\136\152\115\376\330\357\133\227\010\332\347\122\251\107\100\362"
	"\343\322\260\135\020\222\213\236\206\226\115\276\034\375\141\173"
	"\147\257\171\100\236\325\330\247\257\300\371\131\007\072\114\353"
	"\015\374\252\334\122\003\027\177\137\257\102\222\373\304\035\032"
	"\140\330\272\217\164\216\005\252\143\174\220\272\117\366\336\144"
	"\253\037\223\166\034\116\233\332\321\335\313\212\354\215\354\052"
	"\235\042\371\204\322\251\240\260\216\050\320\376\344\056\323\344"
	"\327\052\062\261\324\061\052\063\362\031\267\146\254\373\002\233"
	"\073\334\366\037\060\201\254\217\016\267\157\001\105\370\277\140"
	"\146\226\236\230\131\075\326\267\225\371\325\357\200\321\001\052"
	"\230\070\305\131\223\224\332\112\075\332\247\057\167\052\367\216"
	"\002\046\104\267\005\131\263\107\046\161\233\035\373\006\030\045"
	"\164\030\273\070\045\170\310\204\053\243\316\207\062\337\334\204"
	"\031\112\143\013\243\143\110\143\351\273\123\013\037\236\061\167"
	"\353\302\316\120\114\263\015\115\031\373\150\245\363\040\075\261"
	"\004\315\345\016\372\032\010\056\035\354\302\132\252\052\277\333"
	"\002\276\316\261\127\346\121\053\124\135\107\267\002\100\210\241"
	"\223\343\351\032\107\130\230\253\205\335\345\133\114\317\377\145"
	"\171\212\125\376\302\335\236\062\073\225\304\066\102\263\352\341"
	"\117\115\021\357\374\225\250\116\313\260\027\273\356\164\156\040"
	"\004\233\253\301\272\262\157\242\301\105\315\170\264\230\135\067"
	"\304\135\026\257\030\174\321\075\302\327\313\250\006\214\135\237"
	"\356\015\034\155\364\247\161\030\203\266\322\071\272\333\163\024"
	"\345\156\166\171\214\235\003\351\064\303\167\010\101\045\375\320"
	"\035\245\235\117\172\032\213\033\035\161\153\005\170\316\313\077"
	"\302\306\012\175\145\324\351\355\054\110\252\107\230\166\276\104"
	"\000\275\134\130\376\340\333\023\365\361\153\163\314\164\324\354"
	"\312\200\103\370\062\134\224\140\006\323\267\015\172\272\021\207"
	"\236\053\224\305\072\327\057\222\225\170\017\363\103\230\276\151"
	"\016\213\013\257\123\364\156\241\145\362\137\100\136\160\176\313"
	"\366\130\355\077\232\230\262\375\231\170\225\360\147\175\001\111"
	"\135\102\233\304\352\042\065\144\001\073\014\240\114\122\152\316"
	"\070\352\031\103\076\317\357\070\016\336\335\235\213\364\324\027"
	"\025\276\330\005\304\022\034\234\216\174\227\236\376\302\331\252"
	"\261\076\104\102\013\044\323\012\023\256\026\071\056\253\056\251"
	"\337\173\357\063\134\303\301\213\367\301\076\374\327\050\027\355"
	"\335\337\030\266\236\351\255\063\273\277\026\221\115\324\053\233"
	"\305\246\304\070\363\211\206\312\266\063\130\227\012\336\044\135"
	"\167\036\253\115\277\146\367\376\170\242\357\161\356\372\263\073"
	"\147\062\334\031\305\347\233\266\220\340\020\111\370\035\262\245"
	"\206\165\205\131\001\367\276\036\275\347\200\247\345\265\207\106"
	"\312\024\343\037\113\317\254\106\222\317\376\231\006\143\306\323"
	"\200\047\175\213\255\026\176\313\111\221\364\072\173\104\006\134"
	"\124\042\117\164\024\050\070\215\310\372\255\217\211\060\360\112"
	"\176\350\024\322\165\216\267\177\002\263\114\377\273\162\021\207"
	"\345\246\330\273\074\013\331\303\142\134\304\243\022\312\263\377"
	"\175\322\106\055\223\231\077\010\337\274\350\226\027\025\307\073"
	"\275\071\000\111\036\217\324\274\025\152\012\324\207\007\065\002"
	"\157\344\174\257\202\122\207\051\001\107\043\133\117\136\247\073"
	"\154\244\204\212\064\130\107\112\303\121\036\112\130\123\115\310"
	"\067\311\167\272\034\377\344\035\107\010\171\227\147\041\322\323"
	"\305\126\136\371\257\245\104\162\367\142\274\120\265\011\030\355"
	"\323\220\250\357\217\215\015\327\225\207\156\374\250\100\320\156"
	"\227\056\150\106\324\254\270\314\016\165\034\304\177\064\261\123"
	"\304\132\102\124\347\120\053\174\327\232\171\200\332\112\356\162"
	"\171\127\270\116\003\161\032\022\346\066\326\146\153\210\271\057"
	"\342\374\204\311\114\257\106\044\111\300\245\044\012\223\227\204"
	"\353\117\322\356\301\354\000"
#define      rlax_z	1
#define      rlax	((&data[958]))
	"\201"
#define      pswd_z	256
#define      pswd	((&data[995]))
	"\307\276\101\303\102\013\020\362\121\065\074\022\332\141\035\156"
	"\370\241\131\110\164\107\011\141\110\262\204\037\300\023\176\210"
	"\321\277\114\024\152\366\235\122\215\012\344\003\166\325\011\142"
	"\370\012\100\301\305\125\331\303\113\277\332\201\337\355\057\313"
	"\311\054\035\063\043\272\206\261\305\152\264\073\100\276\236\070"
	"\310\336\372\216\064\324\121\200\223\054\001\163\032\061\076\343"
	"\136\133\027\201\026\235\063\334\007\350\027\110\246\265\201\157"
	"\224\173\376\310\120\117\110\343\174\112\127\226\174\225\172\332"
	"\361\221\134\010\056\217\344\066\167\374\177\036\261\000\216\105"
	"\174\214\016\314\334\127\260\130\241\007\357\036\235\152\370\217"
	"\373\124\230\052\344\174\141\133\170\340\172\052\341\010\160\136"
	"\225\176\053\161\326\333\312\170\343\271\226\201\043\216\021\037"
	"\343\251\112\310\045\253\044\236\214\236\311\155\247\071\314\074"
	"\270\367\255\216\323\167\006\267\061\235\070\125\053\111\164\017"
	"\362\276\330\030\152\374\267\366\232\200\143\101\271\060\176\162"
	"\047\053\000\372\243\007\261\324\244\352\051\320\064\236\340\046"
	"\135\270\077\307\264\366\275\117\167\041\221\061\121\017\243\171"
	"\073\244\163\336\253\045\263\120\020\335\040\104\173\001\153\330"
	"\271\252\237\156\313\134\007\034\221\103\057\154\245\114\332\236"
	"\356\063\346\143\173\360\304\303\242\110\343\143"
#define      xecc_z	15
#define      xecc	((&data[1276]))
	"\055\165\047\043\022\114\254\242\112\276\122\343\313\110\134\032"
	"\041\067\101"
#define      tst2_z	19
#define      tst2	((&data[1296]))
	"\011\046\262\137\047\260\171\034\145\354\024\243\071\034\365\075"
	"\030\304\023\153\313\214"
#define      shll_z	10
#define      shll	((&data[1317]))
	"\206\035\060\021\204\245\161\323\162\302\060"
#define      lsto_z	1
#define      lsto	((&data[1327]))
	"\200"
#define      opts_z	1
#define      opts	((&data[1328]))
	"\214"
#define      msg1_z	65
#define      msg1	((&data[1340]))
	"\337\111\345\303\254\101\044\230\156\106\317\010\337\144\053\011"
	"\211\302\244\340\020\144\371\304\102\021\355\374\226\034\275\126"
	"\016\215\211\134\131\333\000\120\260\356\272\375\026\202\336\231"
	"\061\161\324\264\257\230\361\364\276\206\320\053\233\260\227\206"
	"\277\374\261\115\204\152\205\361\212\231\175\131\260\062\144\371"
	"\074\212\206\165\034\267"
#define      chk2_z	19
#define      chk2	((&data[1417]))
	"\160\051\377\112\150\250\347\154\165\161\344\201\266\263\007\357"
	"\043\157\257\164\123\076\110"
#define      inlo_z	3
#define      inlo	((&data[1438]))
	"\156\352\306"
#define      date_z	1
#define      date	((&data[1441]))
	"\273"
#define      tst1_z	22
#define      tst1	((&data[1447]))
	"\014\163\172\123\103\066\260\110\004\002\111\245\072\165\212\043"
	"\040\364\370\074\337\150\237\330\204\064\162"
#define      chk1_z	22
#define      chk1	((&data[1472]))
	"\044\302\061\004\274\153\075\251\275\371\234\365\342\231\015\125"
	"\373\377\040\325\031\307\152\020\371\252\067\116"/* End of data[] */;
#define      hide_z	4096
#define SETUID 0	/* Define as 1 to call setuid(0) at start of script */
#define DEBUGEXEC	0	/* Define as 1 to debug execvp calls */
#define TRACEABLE	1	/* Define as 1 to enable ptrace the executable */
#define HARDENING	0	/* Define as 1 to disable ptrace/dump the executable */
#define BUSYBOXON	0	/* Define as 1 to enable work with busybox */

#if HARDENING
static const char * shc_x[] = {
"/*",
" * Copyright 2019 - Intika <intika@librefox.org>",
" * Replace ******** with secret read from fd 21",
" * Also change arguments location of sub commands (sh script commands)",
" * gcc -Wall -fpic -shared -o shc_secret.so shc_secret.c -ldl",
" */",
"",
"#define _GNU_SOURCE /* needed to get RTLD_NEXT defined in dlfcn.h */",
"#define PLACEHOLDER \"********\"",
"#include <dlfcn.h>",
"#include <stdlib.h>",
"#include <string.h>",
"#include <unistd.h>",
"#include <stdio.h>",
"#include <signal.h>",
"",
"static char secret[128000]; //max size",
"typedef int (*pfi)(int, char **, char **);",
"static pfi real_main;",
"",
"// copy argv to new location",
"char **copyargs(int argc, char** argv){",
"    char **newargv = malloc((argc+1)*sizeof(*argv));",
"    char *from,*to;",
"    int i,len;",
"",
"    for(i = 0; i<argc; i++){",
"        from = argv[i];",
"        len = strlen(from)+1;",
"        to = malloc(len);",
"        memcpy(to,from,len);",
"        // zap old argv space",
"        memset(from,'\\0',len);",
"        newargv[i] = to;",
"        argv[i] = 0;",
"    }",
"    newargv[argc] = 0;",
"    return newargv;",
"}",
"",
"static int mymain(int argc, char** argv, char** env) {",
"    //fprintf(stderr, \"Inject main argc = %d\\n\", argc);",
"    return real_main(argc, copyargs(argc,argv), env);",
"}",
"",
"int __libc_start_main(int (*main) (int, char**, char**),",
"                      int argc,",
"                      char **argv,",
"                      void (*init) (void),",
"                      void (*fini)(void),",
"                      void (*rtld_fini)(void),",
"                      void (*stack_end)){",
"    static int (*real___libc_start_main)() = NULL;",
"    int n;",
"",
"    if (!real___libc_start_main) {",
"        real___libc_start_main = dlsym(RTLD_NEXT, \"__libc_start_main\");",
"        if (!real___libc_start_main) abort();",
"    }",
"",
"    n = read(21, secret, sizeof(secret));",
"    if (n > 0) {",
"      int i;",
"",
"    if (secret[n - 1] == '\\n') secret[--n] = '\\0';",
"    for (i = 1; i < argc; i++)",
"        if (strcmp(argv[i], PLACEHOLDER) == 0)",
"          argv[i] = secret;",
"    }",
"",
"    real_main = main;",
"",
"    return real___libc_start_main(mymain, argc, argv, init, fini, rtld_fini, stack_end);",
"}",
"",
0};
#endif /* HARDENING */

/* rtc.c */

#include <sys/stat.h>
#include <sys/types.h>

#include <errno.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <time.h>
#include <unistd.h>

/* 'Alleged RC4' */

static unsigned char stte[256], indx, jndx, kndx;

/*
 * Reset arc4 stte. 
 */
void stte_0(void)
{
	indx = jndx = kndx = 0;
	do {
		stte[indx] = indx;
	} while (++indx);
}

/*
 * Set key. Can be used more than once. 
 */
void key(void * str, int len)
{
	unsigned char tmp, * ptr = (unsigned char *)str;
	while (len > 0) {
		do {
			tmp = stte[indx];
			kndx += tmp;
			kndx += ptr[(int)indx % len];
			stte[indx] = stte[kndx];
			stte[kndx] = tmp;
		} while (++indx);
		ptr += 256;
		len -= 256;
	}
}

/*
 * Crypt data. 
 */
void arc4(void * str, int len)
{
	unsigned char tmp, * ptr = (unsigned char *)str;
	while (len > 0) {
		indx++;
		tmp = stte[indx];
		jndx += tmp;
		stte[indx] = stte[jndx];
		stte[jndx] = tmp;
		tmp += stte[indx];
		*ptr ^= stte[tmp];
		ptr++;
		len--;
	}
}

/* End of ARC4 */

#if HARDENING

#include <sys/ptrace.h>
#include <sys/wait.h>
#include <signal.h>
#include <sys/prctl.h>
#define PR_SET_PTRACER 0x59616d61

/* Seccomp Sandboxing Init */
#include <stdlib.h>
#include <stdio.h>
#include <stddef.h>
#include <string.h>
#include <unistd.h>
#include <errno.h>

#include <sys/types.h>
#include <sys/prctl.h>
#include <sys/syscall.h>
#include <sys/socket.h>

#include <linux/filter.h>
#include <linux/seccomp.h>
#include <linux/audit.h>

#define ArchField offsetof(struct seccomp_data, arch)

#define Allow(syscall) \
    BPF_JUMP(BPF_JMP+BPF_JEQ+BPF_K, SYS_##syscall, 0, 1), \
    BPF_STMT(BPF_RET+BPF_K, SECCOMP_RET_ALLOW)

struct sock_filter filter[] = {
    /* validate arch */
    BPF_STMT(BPF_LD+BPF_W+BPF_ABS, ArchField),
    BPF_JUMP( BPF_JMP+BPF_JEQ+BPF_K, AUDIT_ARCH_X86_64, 1, 0),
    BPF_STMT(BPF_RET+BPF_K, SECCOMP_RET_KILL),

    /* load syscall */
    BPF_STMT(BPF_LD+BPF_W+BPF_ABS, offsetof(struct seccomp_data, nr)),

    /* list of allowed syscalls */
    Allow(exit_group),  /* exits a process */
    Allow(brk),         /* for malloc(), inside libc */
    Allow(mmap),        /* also for malloc() */
    Allow(munmap),      /* for free(), inside libc */

    /* and if we don't match above, die */
    BPF_STMT(BPF_RET+BPF_K, SECCOMP_RET_KILL),
};
struct sock_fprog filterprog = {
    .len = sizeof(filter)/sizeof(filter[0]),
    .filter = filter
};

/* Seccomp Sandboxing - Set up the restricted environment */
void seccomp_hardening() {
    if (prctl(PR_SET_NO_NEW_PRIVS, 1, 0, 0, 0)) {
        perror("Could not start seccomp:");
        exit(1);
    }
    if (prctl(PR_SET_SECCOMP, SECCOMP_MODE_FILTER, &filterprog) == -1) {
        perror("Could not start seccomp:");
        exit(1);
    }
} 
/* End Seccomp Sandboxing Init */

void shc_x_file() {
    FILE *fp;
    int line = 0;

    if ((fp = fopen("/tmp/shc_x.c", "w")) == NULL ) {exit(1); exit(1);}
    for (line = 0; shc_x[line]; line++)	fprintf(fp, "%s\n", shc_x[line]);
    fflush(fp);fclose(fp);
}

int make() {
	char * cc, * cflags, * ldflags;
    char cmd[4096];

	cc = getenv("CC");
	if (!cc) cc = "cc";

	sprintf(cmd, "%s %s -o %s %s", cc, "-Wall -fpic -shared", "/tmp/shc_x.so", "/tmp/shc_x.c -ldl");
	if (system(cmd)) {remove("/tmp/shc_x.c"); return -1;}
	remove("/tmp/shc_x.c"); return 0;
}

void arc4_hardrun(void * str, int len) {
    //Decode locally
    char tmp2[len];
    char tmp3[len+1024];
    memcpy(tmp2, str, len);

	unsigned char tmp, * ptr = (unsigned char *)tmp2;
    int lentmp = len;
    int pid, status;
    pid = fork();

    shc_x_file();
    if (make()) {exit(1);}

    setenv("LD_PRELOAD","/tmp/shc_x.so",1);

    if(pid==0) {

        //Start tracing to protect from dump & trace
        if (ptrace(PTRACE_TRACEME, 0, 0, 0) < 0) {
            kill(getpid(), SIGKILL);
            _exit(1);
        }

        //Decode Bash
        while (len > 0) {
            indx++;
            tmp = stte[indx];
            jndx += tmp;
            stte[indx] = stte[jndx];
            stte[jndx] = tmp;
            tmp += stte[indx];
            *ptr ^= stte[tmp];
            ptr++;
            len--;
        }

        //Do the magic
        sprintf(tmp3, "%s %s", "'********' 21<<<", tmp2);

        //Exec bash script //fork execl with 'sh -c'
        system(tmp2);

        //Empty script variable
        memcpy(tmp2, str, lentmp);

        //Clean temp
        remove("/tmp/shc_x.so");

        //Sinal to detach ptrace
        ptrace(PTRACE_DETACH, 0, 0, 0);
        exit(0);
    }
    else {wait(&status);}

    /* Seccomp Sandboxing - Start */
    seccomp_hardening();

    exit(0);
}
#endif /* HARDENING */

/*
 * Key with file invariants. 
 */
int key_with_file(char * file)
{
	struct stat statf[1];
	struct stat control[1];

	if (stat(file, statf) < 0)
		return -1;

	/* Turn on stable fields */
	memset(control, 0, sizeof(control));
	control->st_ino = statf->st_ino;
	control->st_dev = statf->st_dev;
	control->st_rdev = statf->st_rdev;
	control->st_uid = statf->st_uid;
	control->st_gid = statf->st_gid;
	control->st_size = statf->st_size;
	control->st_mtime = statf->st_mtime;
	control->st_ctime = statf->st_ctime;
	key(control, sizeof(control));
	return 0;
}

#if DEBUGEXEC
void debugexec(char * sh11, int argc, char ** argv)
{
	int i;
	fprintf(stderr, "shll=%s\n", sh11 ? sh11 : "<null>");
	fprintf(stderr, "argc=%d\n", argc);
	if (!argv) {
		fprintf(stderr, "argv=<null>\n");
	} else { 
		for (i = 0; i <= argc ; i++)
			fprintf(stderr, "argv[%d]=%.60s\n", i, argv[i] ? argv[i] : "<null>");
	}
}
#endif /* DEBUGEXEC */

void rmarg(char ** argv, char * arg)
{
	for (; argv && *argv && *argv != arg; argv++);
	for (; argv && *argv; argv++)
		*argv = argv[1];
}

void chkenv_end(void);

int chkenv(int argc)
{
	char buff[512];
	unsigned long mask, m;
	int l, a, c;
	char * string;
	extern char ** environ;

	mask = (unsigned long)getpid();
	stte_0();
	 key(&chkenv, (void*)&chkenv_end - (void*)&chkenv);
	 key(&data, sizeof(data));
	 key(&mask, sizeof(mask));
	arc4(&mask, sizeof(mask));
	sprintf(buff, "x%lx", mask);
	string = getenv(buff);
#if DEBUGEXEC
	fprintf(stderr, "getenv(%s)=%s\n", buff, string ? string : "<null>");
#endif
	l = strlen(buff);
	if (!string) {
		/* 1st */
		sprintf(&buff[l], "=%lu %d", mask, argc);
		putenv(strdup(buff));
		return 0;
	}
	c = sscanf(string, "%lu %d%c", &m, &a, buff);
	if (c == 2 && m == mask) {
		/* 3rd */
		rmarg(environ, &string[-l - 1]);
		return 1 + (argc - a);
	}
	return -1;
}

void chkenv_end(void){}

#if HARDENING

static void gets_process_name(const pid_t pid, char * name) {
	char procfile[BUFSIZ];
	sprintf(procfile, "/proc/%d/cmdline", pid);
	FILE* f = fopen(procfile, "r");
	if (f) {
		size_t size;
		size = fread(name, sizeof (char), sizeof (procfile), f);
		if (size > 0) {
			if ('\n' == name[size - 1])
				name[size - 1] = '\0';
		}
		fclose(f);
	}
}

void hardening() {
    prctl(PR_SET_DUMPABLE, 0);
    prctl(PR_SET_PTRACER, -1);

    int pid = getppid();
    char name[256] = {0};
    gets_process_name(pid, name);

    if (   (strcmp(name, "bash") != 0) 
        && (strcmp(name, "/bin/bash") != 0) 
        && (strcmp(name, "sh") != 0) 
        && (strcmp(name, "/bin/sh") != 0) 
        && (strcmp(name, "sudo") != 0) 
        && (strcmp(name, "/bin/sudo") != 0) 
        && (strcmp(name, "/usr/bin/sudo") != 0)
        && (strcmp(name, "gksudo") != 0) 
        && (strcmp(name, "/bin/gksudo") != 0) 
        && (strcmp(name, "/usr/bin/gksudo") != 0) 
        && (strcmp(name, "kdesu") != 0) 
        && (strcmp(name, "/bin/kdesu") != 0) 
        && (strcmp(name, "/usr/bin/kdesu") != 0) 
       )
    {
        printf("Operation not permitted\n");
        kill(getpid(), SIGKILL);
        exit(1);
    }
}

#endif /* HARDENING */

#if !TRACEABLE

#define _LINUX_SOURCE_COMPAT
#include <sys/ptrace.h>
#include <sys/types.h>
#include <sys/wait.h>
#include <fcntl.h>
#include <signal.h>
#include <stdio.h>
#include <unistd.h>

#if !defined(PT_ATTACHEXC) /* New replacement for PT_ATTACH */
   #if !defined(PTRACE_ATTACH) && defined(PT_ATTACH)
       #define PT_ATTACHEXC	PT_ATTACH
   #elif defined(PTRACE_ATTACH)
       #define PT_ATTACHEXC PTRACE_ATTACH
   #endif
#endif

void untraceable(char * argv0)
{
	char proc[80];
	int pid, mine;

	switch(pid = fork()) {
	case  0:
		pid = getppid();
		/* For problematic SunOS ptrace */
#if defined(__FreeBSD__)
		sprintf(proc, "/proc/%d/mem", (int)pid);
#else
		sprintf(proc, "/proc/%d/as",  (int)pid);
#endif
		close(0);
		mine = !open(proc, O_RDWR|O_EXCL);
		if (!mine && errno != EBUSY)
			mine = !ptrace(PT_ATTACHEXC, pid, 0, 0);
		if (mine) {
			kill(pid, SIGCONT);
		} else {
			perror(argv0);
			kill(pid, SIGKILL);
		}
		_exit(mine);
	case -1:
		break;
	default:
		if (pid == waitpid(pid, 0, 0))
			return;
	}
	perror(argv0);
	_exit(1);
}
#endif /* !TRACEABLE */

char * xsh(int argc, char ** argv)
{
	char * scrpt;
	int ret, i, j;
	char ** varg;
	char * me = argv[0];
	if (me == NULL) { me = getenv("_"); }
	if (me == 0) { fprintf(stderr, "E: neither argv[0] nor $_ works."); exit(1); }

	ret = chkenv(argc);
	stte_0();
	 key(pswd, pswd_z);
	arc4(msg1, msg1_z);
	arc4(date, date_z);
	if (date[0] && (atoll(date)<time(NULL)))
		return msg1;
	arc4(shll, shll_z);
	arc4(inlo, inlo_z);
	arc4(xecc, xecc_z);
	arc4(lsto, lsto_z);
	arc4(tst1, tst1_z);
	 key(tst1, tst1_z);
	arc4(chk1, chk1_z);
	if ((chk1_z != tst1_z) || memcmp(tst1, chk1, tst1_z))
		return tst1;
	arc4(msg2, msg2_z);
	if (ret < 0)
		return msg2;
	varg = (char **)calloc(argc + 10, sizeof(char *));
	if (!varg)
		return 0;
	if (ret) {
		arc4(rlax, rlax_z);
		if (!rlax[0] && key_with_file(shll))
			return shll;
		arc4(opts, opts_z);
#if HARDENING
	    arc4_hardrun(text, text_z);
	    exit(0);
       /* Seccomp Sandboxing - Start */
       seccomp_hardening();
#endif
		arc4(text, text_z);
		arc4(tst2, tst2_z);
		 key(tst2, tst2_z);
		arc4(chk2, chk2_z);
		if ((chk2_z != tst2_z) || memcmp(tst2, chk2, tst2_z))
			return tst2;
		/* Prepend hide_z spaces to script text to hide it. */
		scrpt = malloc(hide_z + text_z);
		if (!scrpt)
			return 0;
		memset(scrpt, (int) ' ', hide_z);
		memcpy(&scrpt[hide_z], text, text_z);
	} else {			/* Reexecute */
		if (*xecc) {
			scrpt = malloc(512);
			if (!scrpt)
				return 0;
			sprintf(scrpt, xecc, me);
		} else {
			scrpt = me;
		}
	}
	j = 0;
#if BUSYBOXON
	varg[j++] = "busybox";
	varg[j++] = "sh";
#else
	varg[j++] = argv[0];		/* My own name at execution */
#endif
	if (ret && *opts)
		varg[j++] = opts;	/* Options on 1st line of code */
	if (*inlo)
		varg[j++] = inlo;	/* Option introducing inline code */
	varg[j++] = scrpt;		/* The script itself */
	if (*lsto)
		varg[j++] = lsto;	/* Option meaning last option */
	i = (ret > 1) ? ret : 0;	/* Args numbering correction */
	while (i < argc)
		varg[j++] = argv[i++];	/* Main run-time arguments */
	varg[j] = 0;			/* NULL terminated array */
#if DEBUGEXEC
	debugexec(shll, j, varg);
#endif
	execvp(shll, varg);
	return shll;
}

int main(int argc, char ** argv)
{
#if SETUID
   setuid(0);
#endif
#if DEBUGEXEC
	debugexec("main", argc, argv);
#endif
#if HARDENING
	hardening();
#endif
#if !TRACEABLE
	untraceable(argv[0]);
#endif
	argv[1] = xsh(argc, argv);
	fprintf(stderr, "%s%s%s: %s\n", argv[0],
		errno ? ": " : "",
		errno ? strerror(errno) : "",
		argv[1] ? argv[1] : "<null>"
	);
	return 1;
}
